# Ground AI advice in trusted operations and deny execution authority

Cloud AI may explain observations and select from trusted Operation IDs, but it cannot emit an executable recommendation outside the catalog, update Facts, execute commands, or bypass deterministic risk checks. Only read-only probes establish Facts.
