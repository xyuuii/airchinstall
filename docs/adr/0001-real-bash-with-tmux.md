# Keep the learner in real Bash and compose the UI with tmux

Airchinstall uses tmux panes around an unmodified Bash PTY instead of embedding a terminal emulator or replacing the shell. This preserves authentic command entry and interactive tools while letting mentor and Wiki clients evolve independently.
