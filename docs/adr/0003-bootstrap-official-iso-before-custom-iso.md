# Bootstrap the official Arch ISO before maintaining a custom ISO

The first milestone installs Airchinstall and its TTY dependencies into the official Live environment's volatile overlay. A custom ISO is deferred until the core Shell capture, dynamic fact model and AI guidance have passed QEMU validation.
